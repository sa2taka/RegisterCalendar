div.text-center
  h1 イベントカレンダー

#calendar

br

javascript:
  var data
  data = #{{ event_data.to_json }}
  isLogin = #{{ session[:is_login] }}
  console.log(data)
  $(document).ready(function(){
    var calendar;
    return calendar = new Calendar('#calendar', data, isLogin);
  });

  // ---
  // generated by coffee-script 1.9.2
